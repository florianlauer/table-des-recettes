# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# devenv
.devenv.flake.nix
.devenv
.direnv
!.envrc
.gstack/

# Page de revue du spike : 5,6 Mo, régénérable par `node spike/build-review.mjs`. Elle réencode en
# base64 les photos déjà versionnées dans spike/fixtures/pages.
spike/review-barreau-6.html

# TanStack Start / Nitro
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Vercel : `vercel link` écrit les identifiants d'organisation et de projet ici, et le preset
# Vercel de Nitro y écrit la sortie de build.
.vercel

# impeccable : état de cache régénérable
.impeccable/hook.cache.json
.impeccable/config.local.json

# convex : bundles de skills d'agent, vendorisés en double et régénérables
# via `npx convex ai-files install`
.claude/skills/
.agents/
skills-lock.json

# Spike T13 — 32 rendus pèsent des dizaines de Mo ; seuls ceux du modèle retenu seront commités à la main.
spike13/fixtures/renders/
spike13/review.html

# Personal photographs ingested for an ad-hoc bench campaign. The four committed dishes are
# the reproducible grid; anything else is a page of a copyrighted magazine and this repo is public.
spike13/fixtures/dishes/prod*.jpg
